# shipcheck app env template — infra lives in bus/.env, never here.
# Usage: cp .env.example .env (then fill). Load: set -a; source ../.env; source .env; set +a

STRIPE_AUDIT_LINK=
STRIPE_RESCUE_LINK=
STRIPE_CARE_LINK=
PORT=3000
# DATABASE_URL is set in Dokploy app env (internal Postgres). Absent = local SQLite.
DATABASE_URL=
